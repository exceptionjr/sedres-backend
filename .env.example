# ===========================================
# AMBIENTE DE DESENVOLVIMENTO
# ===========================================
# Descomente a seção que você vai usar:

# --- OPÇÃO 1: Docker Compose (banco local) ---
# DATABASE_URL="postgresql://postgres:SuaSenha@database:5432/sedres_db?schema=public"

# --- OPÇÃO 2: Banco Neon (remoto) ---
# DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# URLs para Prisma (produção Vercel configura automaticamente)
# POSTGRES_PRISMA_URL="postgresql://user:password@host-pooler/database?sslmode=require"
# POSTGRES_URL_NON_POOLING="postgresql://user:password@host/database?sslmode=require"

# ===========================================
# DOCKER COMPOSE (variáveis para o container)
# ===========================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=SuaSenha
POSTGRES_DB=sedres_db

# ===========================================
# JWT
# ===========================================
JWT_SECRET="SuaChaveSecreta"
